//add number variables within an array together
var sum = [1, 2, 3, 4].reduce(add,0);
  function add(a, b) {return a + b;}
console.log(sum); // 10
//multiplying within array
var numbers = [1, 5, 10, 15];
var doubles = numbers.map(function(x) {
   return x * 2;
}
)
console.log(1,doubles)



////OR

var numbers1 = [1, 5, 10, 15];
var triples = numbers1.map(function(x) {
   return x * 3;
}
)
console.log(2, triples)
// long logic line
//combining multiplying two access points with an array
var a=for(i=0;i<(array[0]).length;i++){(for(j=0;j<(array[i][(point A)]).length;j++){var k=Object.keys(array[i][2].forEach(k)(function(x){var l=(Object.keys(array[i][j][(point b)*k]))}))})}
